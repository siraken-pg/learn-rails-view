require 'rails_helper'

describe PostsHelper do
  # pending "add some examples to (or delete) #{__FILE__}"
  describe "string concat" do
    it "one plus one is two" do
      expect(1 + 1).to eq(2)
    end
  end
end
